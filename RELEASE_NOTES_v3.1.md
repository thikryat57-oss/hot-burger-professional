# Hot Burger Professional v3.1 — Production Hardening

## الهدف
مرحلة مراجعة هندسية قبل الإطلاق، تركز على سلامة البيانات، الأمان، وسلوك النظام في الحالات الحدّية.

## التغييرات الرئيسية
- تفعيل SQLite Foreign Keys عبر `PRAGMA foreign_keys = ON`.
- إضافة ترقية كلمات المرور إلى hash مملح مع key stretching، مع دعم ترقية الحسابات القديمة عند أول تسجيل دخول ناجح.
- ترقية قاعدة البيانات إلى v14.
- تحسين توليد أرقام الفواتير باستخدام أعلى رقم رقمي فعلي بدل الاعتماد على آخر سجل.
- تشديد انتقالات حالات KDS لتصبح: جديد → قيد التحضير → جاهز → تم التسليم.
- الحفاظ على جميع وظائف POS، المخزون، الورديات، العملاء، الولاء، KDS، والتقارير.

## ملاحظات الاختبار
لم يتم تشغيل Flutter SDK داخل بيئة العمل الحالية، لذلك يجب تنفيذ `flutter analyze`, `flutter test`, و`flutter build apk --release` على بيئة Flutter فعلية قبل اعتماد الإصدار.
