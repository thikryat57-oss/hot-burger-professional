# Hot Burger Professional v3.9 — Release Candidate

This package is the final staged baseline after the project hardening work.

## Status
**CONDITIONAL RELEASE CANDIDATE**

The project still requires real Flutter/Android build and runtime validation before public production release.

## Important
The source still contains the predictable default manager credential `1234`. Do not deploy publicly with the default credential unchanged.
